import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    tabNav:{
      marginTop:"5%", 
    },
    container: {
      flex: 1,
      alignItems: 'center',
      justifyContent: 'center',
    },
    text: {
      fontSize: 30,
      fontWeight: 'bold',
    },
  });